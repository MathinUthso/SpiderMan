# Runtime dependencies, pinned to the exact versions the test suite passes on.
# Every pin below was verified to have a Linux / CPython 3.12 wheel (Docker image and Render).
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
scipy==1.18.1
numpy==2.5.3
httpx==0.28.1
python-dotenv==1.2.3
